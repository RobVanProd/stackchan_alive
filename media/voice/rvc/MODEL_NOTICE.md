# Stackchan RVC Model Notice

This directory includes the voice archive selected and supplied for the Stackchan: Alive
release. The archive's original `metadata.json` is preserved unchanged and identifies the
Weights.gg model record `clyaxlb9b000eoiqywl68wcrc`, title `joh`, and author metadata
`triceratops`.

The repository's Apache-2.0 license covers the Stackchan: Alive source code and original project
materials. The archive retains its own embedded provenance metadata; downstream users are
responsible for confirming that their intended model use is permitted.
